# PDFTool
# PDFTool  Extract tables from PDF files and convert them into structured datasets.  Features:  ✓ Extract PDF tables   ✓ Convert to DataFrame   ✓ Export CSV   ✓ Export Excel   ✓ Export SQL database   ✓ Generate reports  Workflow:  PDF → Tables → DataFrame → CSV / Excel / SQL
